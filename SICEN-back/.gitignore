# Build del cliente React (generado por SICEN-front → npm run build)
public/

# Avatares de usuario en disco (la BD guarda solo la ruta /uploads/avatars/...)
storage/avatars/*
!storage/avatars/.gitkeep
