# Sistema Centinela (SICEN) — Variables de entorno (FRONT)
#
# Actualmente el front no requiere variables de entorno para operar:
# - El proxy de /api está definido en `vite.config.js`.
# - La auth (JWT) se maneja desde localStorage.
#
# Si más adelante agregás variables VITE_*, copiá este archivo como referencia.

