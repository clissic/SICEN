{
  "name": "sicen-repo",
  "private": true,
  "description": "Monorepo: usar SICEN-back (API) y SICEN-front (React). Ver README.md.",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
